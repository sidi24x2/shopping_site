import Filters from './Filters';
import Hero from './Hero';

function Main() {
  return (
    <section class="main container">
      <Filters />
      <Hero />
    </section>
  );
}

export default Main;
